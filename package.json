{
  "name": "sirupha-package",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.js",
    "start": "docker compose up --build"
  },
  "dependencies": {
    "prompts": "^2.4.2"
  }
}
